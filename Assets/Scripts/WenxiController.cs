using System;
using UnityEngine;

public class WenxiController : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("poopy pants");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
